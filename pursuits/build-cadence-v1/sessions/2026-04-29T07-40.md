---
pursuit: build-cadence-v1
project: reduce-cli-round-trips
session_start: 2026-04-29T07:10:01
session_end: 2026-04-29T07:40:01
actions_completed:
  - Audit skills and CLI for round-trip wastefulness — list every observed loop and every duplicated call, with a proposed fix and effort estimate
  - 'Add bulk-edit CLI subcommands: cadence add-items and cadence check-items (Tier-1 A) — accept --section + repeated --text/--match — with unit tests'
  - Extend mutating commands (check, add-item, set-status, write-marker, add-waiting-for, flag-waiting-for) to include post-mutation actionProgress + dodProgress in their JSON response (Tier-1 B); update existing tests
  - 'Add Notes-section support to add-item (or new add-note) — append paragraphs to ## Notes (Tier-1 C); add unit tests'
  - Rebuild bundle; full test suite passes; backwards-parsing verified against existing 30+ project files
---

# Marker: reduce-cli-round-trips

## Where

Tier-1 round-trip wins shipped end-to-end. Audit complete (8 follow-up actions queued). Tier-1 A: new CLI subcommands cadence add-items and cadence check-items wired in src/cli.ts and src/write/edits.ts. Tier-1 B: addItem/addItems/checkItem/checkItems all return dodProgress + actionProgress in their JSON, eliminating the post-mutate refetch pattern. Tier-1 C: add-item --section notes appends paragraphs to ## Notes (creates the section if absent). 6 new tests added in test/write.test.ts; all 56 tests pass. Bundle rebuilt to 621.4kb. Backwards-parsing verified by running cadence pursuit against the existing 30+ legacy-DoD project files. Eat-our-own-dogfood demo: this session's bookkeeping used check-items (4 checks in 1 call) and add-item --section notes. Project at 5/9 actions done.

## Next

Decide Tier-2 D and E disposition. Three viable paths: (1) ship D only — small ~15 min, adds --include-pursuit flag to cadence project; (2) defer both D and E to a future focused project (A+B+C are the high-leverage wins; D and E are polish); (3) ship both. Recommendation in the conversation was 'ship D, defer E to its own focused project'. After deciding, do action 7 (update consuming skills — promote, complete, pause, cancel, close, start to use the new bulk + progress-returning APIs where they help) then action 9 (user-story validation: replay a representative flow and document turn-count reduction in project Notes), then complete.

## Open

When updating skills (action 7), specifically /complete needs to learn to consume the dodProgress/actionProgress fields returned by check directly rather than re-running cadence project --json. /promote could pre-emptively use the bulk add-items when adding multiple seed actions. Whether to also update other skills not explicitly named in the action (eg /init for its 4 mkdir's, or /reflect for batched waiting-for review) — keep this project's scope tight, capture broader skill-cleanup as a follow-up if needed.
