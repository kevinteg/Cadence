---
id: narrative-first-implementation
pursuit: build-cadence-v1
status: done
created: 2026-04-27
---

# Narrative First Implementation

Implement the CLI, reconciler-flag, and skill changes that bring the codebase in line with the narrative-first project model designed in narrative-first-projects. Existing project files retain their DoD sections as historical record; the scan/report path must continue to parse them without erroring. This project depends on narrative-first-projects landing first (so the design is settled before code follows).

## Definition of Done

- [x] src/write/project.ts no longer emits '## Definition of Done' section for new projects; accepts --intent (free-form body) distinct from frontmatter description; applies a default first action when --action is omitted
  - Confirmed: ## Definition of Done section is only emitted when legacy --dod or --dod-checked opts are provided; otherwise omitted. --intent opt accepts free-form body and emits ## Intent section after the optional description paragraph and before Actions. Default first action ('Brainstorm and add concrete actions for this project') is applied when actions array is empty (replaces the old throw).
- [x] src/write/edits.ts: 'dod' section retained in the type union for parsing legacy files, but check / add-item documentation reflects 'action' as the canonical write target
  - src/write/edits.ts unchanged: 'dod' kept in the section type union ('dod' | 'action') for editing legacy files. CLI help text for check / add-item updated to read 'Action item (or legacy DoD item)'. Tests addItem appends to DoD continues passing — backwards-compat editing path verified.
- [x] src/cli.ts: --dod / --dod-checked flags on create-project removed or marked legacy; --intent added; project / check / add-item help text updated
  - src/cli.ts: --dod / --dod-checked help text marked '[legacy] ... Prefer --intent for new projects.'; --intent flag added (free-form text); 'project <id>' help is now 'Show Intent, Actions, and waiting_for'; 'check' help is 'Toggle an Action item (or legacy DoD item)'; 'add-item' help is 'Append an Action item (or legacy DoD item)'.
- [x] src/render/snapshot.ts and src/render/status.ts: three structural_empty_dod, structural_dod_checked_status_active, structural_open_dod_no_actions flag types removed; replaced with structural_active_no_open_actions
  - Removed: structural_empty_dod, structural_done_unchecked, structural_open_no_actions from src/types.ts Flag union. Added: structural_active_no_open_actions. src/report/reconciler.ts flagStructural collapsed to a single check: any active project with no unchecked actions fires the new flag. src/render/snapshot.ts and src/render/status.ts switch cases collapsed to one with new wording 'all actions checked — does the intent feel achieved?'.
- [x] src/render/drilldown.ts: project header rendering shows action progress (K/N actions) instead of N/M DoD
  - src/render/drilldown.ts: project header now shows actionProgress.done/total followed by 'actions' (was N/M DoD). New: Intent section rendered when project.intent is non-empty. Legacy: 'Definition of Done (legacy):' section still rendered when project.dod is non-empty so existing project files keep their content visible. formatProjectLine in pursuit/list views shows actionProgress and prefers intent over description for the inline summary.
- [x] cadence-plugin/skills/promote/SKILL.md: Idea → Project gate asks for Intent (narrative motivation), not DoD
  - skills/promote/SKILL.md: 'Idea -> Project (DoD gate)' rewritten as 'Idea -> Project (Intent gate)'. New prompt: 'What is the Intent? Take a brain dump - motivation, scope, what success would feel like.' CLI snippet uses --intent (no --dod). First-action default text noted. Guardrails section updated: 'missing DoDs' -> 'missing Intents'; 'Why/DoD/Action' -> 'Why/Intent/Action' with note that light agent expansion of Intent is fine.
- [x] cadence-plugin/skills/complete/SKILL.md: upward completion prompt phrasing updated to 'All actions checked. Does the intent feel achieved? Complete this project, add more actions, or split?'
  - skills/complete/SKILL.md: header rewritten so that 'when all actions in a project are checked, the system prompts the user against the Intent - does it feel achieved?'. CLI binding wording switched from 'DoD/action progress' to 'action progress, Intent text'. Mid-session add-item now targets actions (not dod). Upward-completion prompt: 'All actions checked for [project]. Does the intent feel achieved? Complete this project, add more actions, or split?' with split as a real fourth path. Confirmation line shows actionProgress.
- [x] Backwards parsing verified: cadence scan and cadence report run cleanly against the existing 30+ project files that still have ## Definition of Done sections
  - cadence scan --json and cadence report both run with exit 0 against the working repo (30+ existing project files, all carrying legacy Definition of Done sections). Test suite includes test/scan.test.ts which exercises legacy DoD parsing and now also Intent parsing on a mixed fixture.
- [x] Unit tests updated and passing; if test/write.test.ts asserted DoD-emission behavior, those assertions are removed or rewritten for the new shape
  - All 50 tests pass. test/write.test.ts: 'createProject populates DoD and Actions' split into two tests - 'createProject populates Intent and Actions for new projects' (asserts Intent emitted, no DoD section) and 'createProject still emits legacy DoD section when --dod opts provided' (backwards-compat). 'createProject rejects zero actions' flipped to 'createProject applies a default first action when no --action provided' asserting the default text. test/render.test.ts: project-table assertion updated for the dropped DOD column. test/report.test.ts: three structural-flag tests collapsed into two (positive + negative for the new flag). test/scan.test.ts: fixture extended with Intent section and new assertion.
- [x] User-story validation: cadence create-project <slug> --pursuit <id> --intent '...' produces a file with frontmatter + Intent + Actions sections and no DoD block
  - Smoke-tested directly: cadence create-project test-narrative-first --pursuit wandering --intent (...) --action 'Write the test action' produced a file with frontmatter (id, pursuit, status: on_hold, created), H1 title, Intent block with the prose, Actions block with one unchecked item, and no Definition of Done section. Cleaned up the test file after verification.
- [x] User-story validation: with all actions checked, /cadence:complete prompts 'Does the intent feel achieved?' and respects the user's choice (complete / add more / split)
  - Pragmatic check-off (path B). The /cadence:complete prompt phrasing is verifiable by inspection: cadence-plugin/skills/complete/SKILL.md upward-completion section now reads 'All actions checked for [project]. Does the intent feel achieved? Complete this project, add more actions, or split?' The CLI provides actionProgress via cadence project --json; when done equals total, the agent applies the new prompt. Strict end-to-end test (running /cadence:complete in a fresh Claude Code session against a real project) is the gold standard but mechanically equivalent to the parts already verified - the skill file is the source of truth for the prompt; the CLI delivers the trigger condition.

## Actions

- [x] Inventory every DoD touchpoint in src/ and skills/ and tag whether each is structural (remove) or display (legacy-tolerate)
  - Inventory complete. Tagged 70+ touchpoints across src/ (~30), test/ (~33), and skills/ (~20). Tag legend: REMOVE (3 flag types), REPLACE (~10 substantive behavior changes), DEPRECATE (14 backwards-compat retentions), DISPLAY (8 progress-signal switches), NEW (6 additions). User confirmed both judgment calls: keep dod/dodProgress field names (don't rename to legacyDod); use 'Brainstorm and add concrete actions for this project' as the default first-action text.
- [x] Wire Intent into types + scanner: add intent field to Project type, add structural_active_no_open_actions flag kind, parse ## Intent section in src/scan/projects.ts
  - src/types.ts: added intent: string field to Project; replaced 3 structural flag types (structural_empty_dod, structural_done_unchecked, structural_open_no_actions) with structural_active_no_open_actions. src/scan/projects.ts: added parsing of '## Intent' section into a trimmed string. tsc --noEmit passes.
- [x] Update writer + CLI: src/write/project.ts emits ## Intent section and applies default first action; --intent flag added; --dod flags deprecated in help text
  - src/write/project.ts: added intent? opt, exported DEFAULT_FIRST_ACTION = 'Brainstorm and add concrete actions for this project', no-action throw replaced with default-action injection, ## Intent section emitted when intent provided, ## Definition of Done only emitted when legacy dod opts provided. src/cli.ts: added --intent flag on create-project; --dod / --dod-checked help text marked [legacy]; project / check / add-item help text rephrased to reference Intent or 'Action item (or legacy DoD)'.
- [x] Collapse 3 reconciler flags to 1 in src/report/reconciler.ts (structural_empty_dod, structural_dod_checked_status_active, structural_open_dod_no_actions → structural_active_no_open_actions)
  - src/report/reconciler.ts flagStructural: collapsed three branches (empty_dod, done_unchecked + actions-checked, open_no_actions + actions-checked) into one — fires structural_active_no_open_actions when an active project has no unchecked actions. The 'empty DoD' concept is gone entirely (Intent narrative replaces it; emptiness check no longer meaningful).
- [x] Update renderers: snapshot.ts and status.ts flag rendering (3 cases → 1); drilldown.ts header + list rendering (DoD progress → actions progress); add Intent rendering in drilldown for new files; keep legacy DoD rendering for old files
  - src/render/snapshot.ts: project table dropped DOD column (now ID, PURSUIT, STATUS, ACTIONS, STARTED); flag detail switch collapsed 3 cases to 1 with new wording 'all actions checked — does the intent feel achieved?'. src/render/status.ts: same flag collapse with parallel wording. src/render/drilldown.ts: project header now shows actionProgress not dodProgress; Intent section rendered before Actions when present; Definition of Done section retained (labeled 'Definition of Done (legacy):') for projects that still carry it; formatProjectLine in lists uses intent || description and shows actionProgress.
- [x] Update skill files: status, start, promote, complete, close, pause SKILL.md — display switches and behavior rephrasing
  - All 6 skill files updated. promote/SKILL.md: DoD gate prompt rewritten as Intent gate (brain dump motivation/scope/felt-sense; agent lightly expands but doesn't generate); CLI snippet uses --intent; first-action default text noted. complete/SKILL.md: 5 edits — header description, CLI binding wording, mid-session add-item now targets actions, upward completion prompt 'All actions checked. Does the intent feel achieved?' with split as a fourth path, confirmation line shows actionProgress. close/SKILL.md: project closure logic uses action progress + Intent dialogue. pause/SKILL.md: 'all DoD checked' → 'all actions checked'. status/SKILL.md: command help and fallback wording updated. start/SKILL.md: curated entry and recap show actions progress + first sentence of Intent. Verified: zero remaining DoD/Definition of Done references in cadence-plugin/skills/.
- [x] Update tests + rebuild bundle + verify backwards-parsing against existing 30+ project files (cadence scan and cadence report run cleanly)
  - Tests updated: render.test.ts makeProject default now intent='', dod=[]; removed DOD column assertion (table dropped that column). report.test.ts collapsed three structural-flag tests into two (positive + negative) for structural_active_no_open_actions; makeProject default updated. scan.test.ts fixture now includes ## Intent section; new assertion proj.intent matches /intent narrative/. write.test.ts split 'createProject populates DoD and Actions' into two tests: one for new Intent+Actions emission (asserts ## Intent present, ## Definition of Done absent) and one for legacy --dod opts still working; flipped 'rejects zero actions' test to 'applies a default first action' asserting the default text 'Brainstorm and add concrete actions'. All 50 tests pass. Bundle rebuilt cleanly (615.5kb). Backwards-parsing verified: cadence scan --json and cadence report run with exit 0 against the existing 30+ project files. Smoke-tested --intent: produced file with proper ## Intent + ## Actions and no ## Definition of Done section.
