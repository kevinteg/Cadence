# Python build dependencies for the GitHub Pages wiki site
# (.github/workflows/pages.yml builds wiki/ → Pages via MkDocs Material).
# Pinned for reproducible deploys; material capped < 10 to stay clear of
# the announced MkDocs 2.0 / Material v10 transition.
mkdocs-material>=9.5,<10
mkdocs-roamlinks-plugin>=0.3.2
